# This is a Databricks asset bundle definition for rds_data_migration_1.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: rds_data_migration_1
  uuid: e84fae1a-4d34-469d-a0a8-d0943142c586

include:
  - resources/*.yml
  - resources/*/*.yml

artifacts:
  rds_data_migration_1:
    type: whl
    build: python3 -m build
    path: .

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-a460ab68-eabd.cloud.databricks.com
    variables:
      catalog: sparkling
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: https://dbc-a460ab68-eabd.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/huynguyen30593@outlook.com to make sure we only have a single copy.
      root_path: /Workspace/Users/huynguyen30593@outlook.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: sparkling
      schema: prod
    permissions:
      - user_name: huynguyen30593@outlook.com
        level: CAN_MANAGE
